<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysporting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySporting">
        <activity
            android:name=".EditSportResultActivity"
            android:label="Edit result"
            android:exported="false" />
        <activity
            android:name=".SportPlaceDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".AddNewSportResultActivity"
            android:exported="false"
            android:label="Add new result" />
        <activity
            android:name=".MyResultsActivity"
            android:exported="false"
            android:label="@string/title_activity_my_results"
            android:theme="@style/Theme.MySporting.NoActionBar" />
        <activity
            android:name=".SportPlacesActivity"
            android:exported="false"
            android:label="Sport places" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>